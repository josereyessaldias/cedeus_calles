<h3>Productos</h3>
<% @products.each do |product| %>
	<%= product.titulo %> <%= link_to 'borrar', product_path(product), method: :delete %> <br>
<% end %>

<%= link_to 'Ingresar nuevo product', new_product_path %>