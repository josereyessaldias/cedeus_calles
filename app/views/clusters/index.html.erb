<h3>Clusters</h3>
<br>

<% @clusters.each do |cluster| %>
	<%= cluster.name %> <%= link_to 'borrar', cluster_path(cluster), method: :delete %> <br>
<% end %>

<br>
<%= link_to 'Crear nuevo cluster', new_cluster_path, :class => 'btn btn-dark btn-sm' %>